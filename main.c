#include <stdio.h>
#include <stdlib.h>
#include "drw.h"


int main() {
    printf("Implement this stuff!!\n");    
    vector_clock *p1;
    p1 = (vector_clock*)calloc(1, sizeof(vector_clock));
    printf("p1 id: %d\n", p1->id);
}
